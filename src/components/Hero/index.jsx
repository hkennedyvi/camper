import React, { Component } from 'react';
import './hero.scss';

class Hero extends Component {
    render() {
        return (
            <section className="hero">
           
            </section>
        )
    }
}

export default Hero;